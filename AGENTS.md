# AGENTS.md

このリポジトリは Accounting State Model (ASM) の理論と、その検証記録を管理する。

## 正本の範囲

- `theory/` 全体を ASM の正本とする。
- `README.md` は正本の代替ではなく、全体の地図とする。
- `courses/<order>-<course-id>/` は情報源を明記した学習・検証ノートであり、正本ではない。
- `docs/legacy/` は過去版の移行スナップショットであり、更新しない。

## Codex の基本ワークフロー

1. 新しい講座・文書を扱うときは、最初に情報源を特定し、該当する course directory のノートを作成または編集する。
2. 既存の `theory/` だけで講座内容を説明できるか検証する。
3. 具体例、教材固有の言い回し、未検証の仮説は、該当する course directory に残す。
4. 複数の講座・勘定・期間、必要に応じて複数の情報源にまたがって成立し、既存定義と整合する内容だけを `theory/` へ昇格する。
5. Theory を更新する場合は、関係する最小限のモジュールだけを変更し、Course Note の「Theory更新候補」を更新する。
6. 反例や未解決問題を、理論に合うように消さない。`courses/` に明記し、理論の修正材料として残す。

最重要ルールは次のとおり。

> 情報源を明記した courses を先に編集し、情報源を越えて一般化できる内容だけ theory へ昇格する。

## Course Note

- 新規ノートは `courses/TEMPLATE.md` の10見出しを保つ。
- YAML front matter は原則として `source`、`course`、`url` の3項目だけにする。
- 複数ページを参照する場合だけ `related_urls` を追加する。
- 元ページの URL は本文へ埋め込むだけでなく、必ず YAML の `url` に記録する。
- 「現行ASMで説明できるか」は `Yes`、`Partially`、`No` のいずれかを明記する。
- Theory へのリンクはリポジトリ内の相対リンクにする。
- 「Theory更新候補」がない場合も見出しを削除せず、「なし」と記す。

## 表記

- 構造図・処理フローの標準は Mermaid とする。
- インライン数式は `$...$`、別行数式は `$$...$$` を使う。
- バックスラッシュ括弧型の LaTeX 区切りは使わない。
- 借方・貸方と数学的な正負を混同しない。
- Stock 状態には原則として $s(t)$、期間 Flow には $R(I)$、$C(I)$、$P(I)$ を使う。
- 現実、会計状態、帳簿表現、報告表現の各レイヤーを混同しない。

## Theory 更新の判定

Theory へ昇格できる内容は、少なくとも次を満たすこと。

- 特定の講座、情報源、勘定科目だけに依存しない。
- 既存の記号・定義と矛盾しないか、矛盾を明示的に解消する。
- 例ではなく、定義・命題・制約・写像として表現できる。
- どの Course Note から得られた洞察か追跡できる。
- 未検証の推測である場合、その状態を明記する。

## 変更時の確認

- Markdown リンクが存在すること。
- Mermaid のコードフェンスが閉じていること。
- LaTeX の区切りが対応していること。
- `README.md` の Theory Modules / Course Notes 一覧と実ファイルが一致すること。
- Course Note の YAML にある `url` と、実際に参照したページが一致すること。
- 用語や記号を追加した場合、必要に応じて `glossary.md` を更新すること。
